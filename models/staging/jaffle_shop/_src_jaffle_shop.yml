sources:
  - name: jaffle_shop
    description: raw jaffle shop data
    database: raw
    schema: jaffle_shop
    tables:
      - name: customers
        description: raw customers data
        columns:
          - name: id
            description: primary key
            data_tests:
              - unique
              - not_null
      - name: orders
        config:
          freshness:
            warn_after:
              count: 100
              period: day
            error_after: 
              count: 1000
              period: day
          loaded_at_field: _etl_loaded_at